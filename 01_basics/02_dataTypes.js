"use strict"; //Treat all JS code as newer version

// alert(3 + 3); => This will not be executed as we are using nodejs not browser

console.log(3 
    + 3) //=> We should avoid this type of code as our focus on code readability should be high

let name = "Shadab";
let age = 22;
let isLoggedIn = true;


//number => range => 2 raised to the power of 53
//bigint
//string => "" => anything under double/single quotes
//boolean => true/false
//null => standalone value => if nothing is stored
//undefined => variable that has not been assigned
//symbol => used for uniqueness


//object

console.log(typeof "Shadab");
console.log(typeof null); //=> null is of OBJECT DataType
console.log(typeof undefined); //=> undefined is of undefined DataType